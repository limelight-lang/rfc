# Architecture — knowledge map

Who knows what and is responsible for what. The written form of the
"design before code" rule.

> **Placeholder.** This repo currently holds specification prose, not
> code, so there are no modules to map yet. The first real entries will
> describe the fact-base tooling (extractor, store, checker) once its
> boundaries are designed. Until then the RFC's own structure is the map
> — see [../README.md](../README.md).

| Module | Responsible for | Knows | Does not know | Depends on |
|---|---|---|---|---|
| _(none yet)_ | | | | |
