# Interop

Cross-language integration at the IR level.

Covers how Limelight's IR interoperates with IRs from other languages and compilers. This includes ABI compatibility, calling conventions across language boundaries, type mapping between foreign type systems, and the design of interop primitives that allow PHP code to call into — and be called from — other languages without going through a high-level FFI boundary.
