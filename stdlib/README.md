# Stdlib

Standard Library — built-in functions, classes, and modules available to PHP programs out of the box.

Covers the design and implementation of core PHP built-ins: string, array, math, date, and other standard modules.
